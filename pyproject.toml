[project]
name = "barquitos"
version = "0.1.0"
description = "LNG vessel tracking dashboard"
requires-python = ">=3.12"
dependencies = [
    "dash>=4.3.0",
    "dash-leaflet>=1.1.3",
    "pandas>=3.0.3",
]
